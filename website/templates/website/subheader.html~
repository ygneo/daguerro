      <div id="category">
        <div id="imgCat">
          <img src="{{ category.photo.get_titulo_categoria_url }}" alt="Botánica"/>
        </div>
        <div id="title">
          <div id="return">
            <img src="{{ MEDIA_URL }}/website/img/assets/undo.png" alt="Botánica" title="Botánica"/> 
            <a href="/">Portada</a>
          </div>
          <h2>{{ category.title }}</h2>
          <h3>{{ category.description }}</h3>
        </div>
        <div id="tools">
          <form id="form_cat_search">
            <fieldset>
              <input type="text" name="cat_search" value="Buscar en {{ category.title }}"/>
              <input type="image"  src="{{ MEDIA_URL }}/website/img/assets/search.png" value="Buscar" alt="Buscar" title="Buscar"/>
            </fieldset>            
          </form>
        </div>
      </div>
